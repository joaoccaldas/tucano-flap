{
  "name": "tucano-flap",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && esbuild src/main.ts --bundle --outfile=dist/bundle.js --format=esm --minify=false --sourcemap=external && cp index.html dist/ && mkdir -p dist/sprites && cp -r public/sprites/* dist/sprites/ 2>/dev/null || true",
    "preview": "vite preview",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "eslint src --ext .ts,.js",
    "audit:perf": "node scripts/perf-audit.js",
    "eval:game": "node scripts/evaluate-game.js"
  },
  "devDependencies": {
    "@types/node": "^20",
    "esbuild": "^0.28.0",
    "eslint": "^8",
    "typescript": "^5",
    "vite": "^5",
    "vitest": "^1"
  }
}
